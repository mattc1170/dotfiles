;; This file is used to define customizable variables that are needed
;; by the .emacs file.  Reasonable defaults have been selected for
;; the permanent repository copy based upon look and feel in an
;; Ubuntu environment on a 1400x1050 laptop screen, but these values 
;; should be changed as needed to accomodate local environment
;; idiosyncracies.

;; local-linux-font :  Which font looks best for the environment.
(setq local-linux-font "Monospace-10")
